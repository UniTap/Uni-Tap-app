import 'package:flutter/material.dart';

class CateenPage extends StatelessWidget {
  const CateenPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("canteen"),
    );
  }
}
